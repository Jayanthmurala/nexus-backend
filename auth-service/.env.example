# Auth Service Environment
PORT=4001
NODE_ENV=development

# Database (Postgres)
# Neon Postgres direct connection (use a service-isolated schema)
# Replace the credentials/host/db and keep schema=authsvc or pick your own schema name
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DB?sslmode=require&schema=authsvc"

# JWT (RS257)
AUTH_JWT_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"
AUTH_JWT_PUBLIC_KEY="-----BEGIN PUBLIC KEY-----\n...\n-----END PUBLIC KEY-----"
AUTH_JWT_KID="auth-key0"
AUTH_JWT_ISSUER="nexus-auth"
AUTH_JWT_AUDIENCE="nexus"
AUTH_JWT_ACCESS_EXPIRES_IN="16m"
AUTH_JWT_REFRESH_EXPIRES_IN="31d"

# Cookies
# For localhost, leave COOKIE_DOMAIN unset to avoid invalid cookie domain issues.
# In production, set to your apex domain, e.g. .example.com
# COOKIE_DOMAIN=

# Frontend origin for building verification/reset URLs
FRONTEND_BASE_URL=http://localhost:3000

# Redis
REDIS_URL=redis://default:Z45okMQXWCkMn2aG6YwvvOmCZQGW2p2i@redis-15521.c11.us-east-1-2.ec2.redns.redis-cloud.com:15521

# One-time token expirations
EMAIL_VERIFICATION_EXPIRES_IN="24h"
PASSWORD_RESET_EXPIRES_IN="1h"

# App + Mail settings
APP_NAME="Nexus"
EMAIL_FROM="Nexus <no-reply@example.com>"
SUPPORT_EMAIL="support@example.com"

# SMTP (leave SMTP_HOST empty to auto-use Ethereal in dev)
# https://ethereal.email/ provides a fake SMTP for previews
# When using a real SMTP, set the following:
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_user
# SMTP_PASS=your_password
# Optional seed admin
# ADMIN_EMAIL="admin@example.com"
# ADMIN_PASSWORD="change_me_strong_password"
# ADMIN_DISPLAY_NAME="Head Admin"

# Service URLs for inter-service communication
PROFILE_SERVICE_URL="http://localhost:4002"
